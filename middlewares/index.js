const errorHandle = require('./errorHandle');
const validateLoging = require('./validateLogin');
const validateAddSpeaker = require('./validateAddSpeaker');
const tokenValidation = require('./tokenValidation');

module.exports = {
  errorHandle,
  validateLoging,
  validateAddSpeaker,
  tokenValidation,
};